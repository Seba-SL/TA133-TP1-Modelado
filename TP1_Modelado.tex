\documentclass[a4paper]{article}
\usepackage[margin=1.5cm,top=1.5cm,bottom=1.5cm,a4paper]{geometry}
\usepackage{graphicx} % Required for inserting images
\usepackage{titlesec} % Required for customizing section titles
\usepackage[margin=1.5cm,top=1.5cm, bottom=1.5cm]{geometry} % Ajusta los márgenes aquí
\usepackage{multicol} % Required for multicols environment
\usepackage{parskip}
\usepackage{etoolbox}
\usepackage{tcolorbox}
\usepackage{float}
\usepackage{amsmath}
\usepackage{dingbat}
\usepackage{booktabs} 
\usepackage{caption}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amssymb}
\usepackage[spanish]{babel}

\addto\captionsspanish{
	\renewcommand{\tablename}{Tabla}
}

\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning}
\usetikzlibrary{babel}

\usepackage{csquotes}

\usepackage[
backend=biber,
style=apa,
citestyle=apa
]{biblatex}

\addbibresource{referencias.bib}





% Personalización de la sección para que comience desde la izquierda
\titleformat{\section}[hang]{\normalfont\Large\bfseries}{\thesection}{1em}{}

\begin{document}
	\setcounter{page}{1}
	\begin{minipage}[h]{1\textwidth}
		
		\title{\includegraphics[height = 1.5cm]{img/logofiuba.png} % Logo en la izquierda del encabezado  
			\\(TB070)Control \\Trabajo Práctico I : Modelado }
		\author{106213 Sebastián Lazo  (slazo@fi.uba.ar) }
		\date{2 de Abril 2026}
		\maketitle
		
	\end{minipage}

 \newpage
 
 \section{Introducción}
 
 El presente trabajo práctico tiene como objetivo ejercitar los conceptos fundamentales de modelado y análisis de sistemas dinámicos vistos en la materia Control. En particular, se busca representar sistemas físicos mediante ecuaciones diferenciales y describir su comportamiento utilizando la formulación en espacio de estados.
 
 A lo largo del trabajo se desarrollan los modelos matemáticos correspondientes a distintos sistemas, identificando las variables de estado, las entradas y las salidas, y obteniendo las ecuaciones que describen su evolución temporal. Asimismo, se emplean herramientas de simulación numérica mediante software como MATLAB, Octave o Scilab, junto con los entornos gráficos Simulink o Xcos, con el objetivo de verificar y analizar el comportamiento dinámico de los modelos obtenidos.
 
 El trabajo se realiza de manera individual, siguiendo las pautas de presentación establecidas por la cátedra. Se incluye tanto el desarrollo teórico de los modelos como las implementaciones computacionales utilizadas para su simulación, debidamente documentadas para facilitar su comprensión y evaluación.
 
 
 \section{Masa-Resorte-Fricción}
 
 Se modelan un sistemas mecanicos traslaciónales con elementos concentrados que ejercen fuerzas, resistencias, acumulan y descargan energía mecánica. 
 
 \subsection{Un grado de libertad}
 
 El primer modelo se centra en una masa sostenida a dos puntos fijos sin movimiento, el superior por medio de un resorte e inferior por medio de un amortiguador.
 
 \subsubsection{Enunciado}
 
 Considere el sistema masa--resorte con fricción mostrado en la Figura \ref{fig:masaresorte1gradolib}. 
 Se pide:
 
Resolución analítica
 
 \begin{enumerate}
 	\item Obtener una descripción del sistema en \textbf{espacio de estados}.
 	
 	\item Obtener la \textbf{función de transferencia} del sistema de dos maneras:
 	\begin{enumerate}
 		\item Aplicando la transformada de Laplace a la ecuación diferencial que describe la dinámica del sistema.
 		\item A partir de la descripción en espacio de estados, es decir, utilizando las matrices $A$, $B$, $C$ y $D$ para obtener la función de transferencia.
 	\end{enumerate}
 	Verificar que ambos resultados coincidan.
 	
 	\item Obtener analíticamente la \textbf{respuesta al escalón} desde la entrada $u$ hasta la salida $y$, utilizando la transformada de Laplace.
 	
 	\item Obtener la \textbf{respuesta a condiciones iniciales} en posición y velocidad mediante la exponencial matricial. 
 	La exponencial matricial puede calcularse utilizando la transformada de Laplace u otro método equivalente.
 \end{enumerate}

Verificación mediante MATLAB/SCILAB
 
 \begin{enumerate}
 	\item Verificar los resultados obtenidos utilizando los comandos \texttt{ss} y \texttt{tf}/\texttt{zpk}. 
 	Verificar también la exponencial matricial utilizando el \textit{Symbolic Toolbox} y la función \texttt{ilaplace}.
 	
 	\item Obtener las respuestas al \textbf{impulso} y al \textbf{escalón} mediante \textit{Simulink/Xcos} y también utilizando el \textit{Control Toolbox} de MATLAB/SCILAB, demostrando el uso de los comandos \texttt{step}, \texttt{initial} e \texttt{impulse}.
 \end{enumerate}
 
\begin{figure}[H]
	\centering
	
	\begin{minipage}{0.25\textwidth}
		\centering
		\includegraphics[width=\linewidth]{img/masa_resorte_1grado_Lib}
	\end{minipage}
	\hfill
	\begin{minipage}{0.65\textwidth}
		
		Planteando el diagrama de cuerpo libre de la masa, tomando como referencia el punto de equilibrio donde se cumple (\ref{equation:equilibrio_1.1}) : 
		
		\begin{equation}\label{equation:equilibrio_1.1}
			u(t) = 0 , y(t) = y_{eq} = 0 , \frac{d^2 y}{dt^2 } = \frac{dy}{dt} = 0
		\end{equation}
	
	    Se plantea la sumatoria de fuerzas en m obteniendo (\ref{equation:sumatoriadeFuerzas_inicial}):
	    
	    \begin{equation}\label{equation:sumatoriadeFuerzas_inicial}
	    	\sum F = F_ g - F_k - F_b + F_u = m \frac{d^2 y}{dt^2 }
	    \end{equation}
		 
		\centering
		\small
		\begin{tabular}{ll}
			\toprule
			Constante & Descripción \\
			\midrule
			$m  = 1 kg$ & Masa \\
			$k = 1 N/m $ & Constante del resorte \\
			$b =  1 N/ms$ & Fricción viscosa \\
			$u(t) , N$ & Fuerza aplicada \\
			$y(t) , m$ & Desplazamiento \\
			\bottomrule
			
		
		\end{tabular}
	\end{minipage}
	
	\caption{Sistema masa–resorte–amortiguador y parámetros del modelo}
	\label{fig:masaresorte1gradolib}
	
\end{figure}

Cada fuerza cumple la relación:

\begin{equation}
	F_g = m\cdot g  ; F_k = k \cdot y(t) ;  F_b = b \cdot \frac{dy}{dt} ; F_u = u(t) 
\end{equation}


Debido que en equilibrio la propia sumatoria, al cumplirse (\ref{equation:equilibrio_1.1}) se obtiene:

   \begin{equation}\label{equation:sumatoriadeFuerzas_inicial_en_eq}
	\sum F = m\cdot g -k \cdot (y(t) =  0) = m \frac{d^2 y}{dt^2 } = 0 
	\end{equation}

Por lo tanto $mg = 0$ siendo constante y se llega a la siguiente expresión.

 \begin{equation}\label{equation:sumatoriadeFuerzas_inicial_conmg0}
	\sum F = - F_k - F_b + F_u = m \frac{d^2 y}{dt^2 }
\end{equation}



\begin{equation}\label{equation:sumatoriadeFuerzas_inicial_conmg0_2}
	 F_u = m \frac{d^2 y}{dt^2 } + F_k + F_b 
\end{equation}

Finalmente se llega a la ecuación diferencial caracteristica del sistema, de orden $2^{do}$ (\ref{equation:sumatoriadeFuerzas_inicial_conmg0_3}).

\begin{equation}\label{equation:sumatoriadeFuerzas_inicial_conmg0_3}
	u(t) = m \frac{d^2 y}{dt^2 } + k \cdot y(t) + b \cdot \frac{dy(t)}{dt}
\end{equation}


Con dicha expresión podemos comenzar a resolver el primer item, obteniendo una descripción del sistema en \textbf{espacio de estados}.

El \textbf{estado} de un sistema dinámico es el conjunto más pequeño de variables 
(denominadas \textit{variables de estado}) tal que el conocimiento de dichas variables 
en un instante $t = t_0$, junto con el conocimiento de la entrada para todo 
$t \geq t_0$, determina completamente el comportamiento del sistema para 
cualquier $t \geq t_0$. Las variables de un sistema dinámico son las variables que constituyen el menor conjunto de variables que determinan el estado del sistema dinámico.Si se necesitan n variables de estado para describir completamente el comportamiento de un sistema dado, entonces esas n variables de estado se pueden considerar como las n componentes de un vector, conocido como vector de estados.

El espacio $n$-dimensional cuyos ejes de coordenadas están formados por 
$x_1, x_2, \ldots, x_n$, donde $x_1, x_2, \ldots, x_n$ son las variables de 
estado, se denomina \textbf{espacio de estados}. 

Cualquier estado del sistema puede representarse como un punto en dicho 
espacio de estados.

Los sistemas dinámicos deben contener elementos que ``recuerden'' los valores 
de la entrada para $t \geq t_1$. En sistemas de control en tiempo continuo, 
los integradores actúan como dispositivos de memoria. Por lo tanto, las salidas 
de dichos integradores pueden considerarse como las variables que describen el 
estado interno del sistema dinámico. Así, las salidas de los integradores sirven 
como \textit{variables de estado}.

El número de variables de estado necesarias para describir completamente la 
dinámica del sistema es igual al número de integradores presentes en el mismo.

Considérese un sistema de múltiples entradas y múltiples salidas con $n$ 
integradores. Supóngase además que existen $r$ entradas 
$u_1(t), u_2(t), \ldots, u_r(t)$ y $m$ salidas 
$y_1(t), y_2(t), \ldots, y_m(t)$. Se definen las $n$ salidas de los integradores 
como variables de estado:

\[
x_1(t), x_2(t), \ldots, x_n(t)
\]

Entonces el sistema puede describirse mediante

\begin{align}
	\dot{x}_1(t) &= f_1(x_1,x_2,\ldots,x_n;\,u_1,u_2,\ldots,u_r;\,t) \\
	\dot{x}_2(t) &= f_2(x_1,x_2,\ldots,x_n;\,u_1,u_2,\ldots,u_r;\,t) \\
	&\vdots \\
	\dot{x}_n(t) &= f_n(x_1,x_2,\ldots,x_n;\,u_1,u_2,\ldots,u_r;\,t)
\end{align}

Las salidas del sistema se obtienen mediante

\begin{align}
	y_1(t) &= g_1(x_1,x_2,\ldots,x_n;\,u_1,u_2,\ldots,u_r;\,t) \\
	y_2(t) &= g_2(x_1,x_2,\ldots,x_n;\,u_1,u_2,\ldots,u_r;\,t) \\
	&\vdots \\
	y_m(t) &= g_m(x_1,x_2,\ldots,x_n;\,u_1,u_2,\ldots,u_r;\,t)
\end{align}


Dado este sistema defino como variables de estado la distancia de la masa y la velocidad de esta (\ref{equation:variablesdeestado_1_1}):


\begin{equation}\label{equation:variablesdeestado_1_1}
	x_1(t) = y(t) \quad \wedge \quad x_2(t) = \frac{dy(t)}{dt} = \dot{y}
\end{equation}


Las cuales cumplen $ x_2(t) = \dot{X_1(t)} $ y $\dot{x_2} = \frac{u(t) }{m} - \frac{b x_2}{m} - \frac{k x_1}{m}$



y de acuerdo a lo extraído del libro, necesito expresar el vector :

\begin{equation}
	\begin{bmatrix}
		\dot{x}_1(t) \\
		\dot{x}_2(t)
	\end{bmatrix}
	=
	\begin{bmatrix}
		0 & 1 \\
		-\dfrac{k}{m} & -\dfrac{b}{m}
	\end{bmatrix}
	\begin{bmatrix}
		x_1(t) \\
		x_2(t)
	\end{bmatrix}
	+
	\begin{bmatrix}
		0 \\
		\dfrac{1}{m}
	\end{bmatrix}
	u(t)
\end{equation}

y la salida del sistema:

\begin{equation}
	y =
	\begin{bmatrix}
		1 & 0
	\end{bmatrix}
	\begin{bmatrix}
		x_1 \\
		x_2
	\end{bmatrix}
\end{equation}


Dando así las matrices que caracterizan el sistema A: Matriz de estado, B: Matriz de entrada, C : Matriz de salida, D : Matriz de transmisión directa.

\begin{equation}
	\begin{aligned}
		\dot{x} &= 
		\underbrace{
			\begin{bmatrix}
				0 & 1 \\
				-\dfrac{k}{m} & -\dfrac{b}{m}
		\end{bmatrix}}_{A}
		x
		+
		\underbrace{
			\begin{bmatrix}
				0 \\
				\dfrac{1}{m}
		\end{bmatrix}}_{B}
		u \\[6pt]
		y &=
		\underbrace{
			\begin{bmatrix}
				1 & 0
		\end{bmatrix}}_{C}
		x
		+
		\underbrace{0}_{D}u
	\end{aligned}
\end{equation}

Con expresar los vectores de estado, su derivada y salida, ya se a respondido el punto a).

Para el punto b) se requiere obtener la transferencia, aplicando Laplace a la ecuación diferencial de la dinámica por un lado y a partir de la descripción en espacio de estados por el otro, es decir pasando de las matrices A,B,C,D a la transferencia. Verificar que den igual.

Primero partiendo de la ecuación diferencial (\ref{equation:deriv_laplace}).

\begin{equation}\label{equation:ecDif_hallar_H}
	u(t) = m \frac{d^2 y}{dt^2 } + k \cdot y(t) + b \cdot \frac{dy(t)}{dt}
\end{equation}


Aplicando la transformada de Laplace, recordando la propiedad ():

\begin{equation}\label{equation:deriv_laplace}
	\mathcal{L}\left\{\frac{d}{dt}f(t)\right\} = sF(s) - f(0)
\end{equation}


\begin{equation}\label{equation:ecDif_hallar_H_2}
	U(s) = m \cdot s^2 \cdot Y(s) + b \cdot s \cdot Y(s) + k \cdot Y(s)
\end{equation}

Obteniendo:


\begin{equation}\label{equation:ecDif_hallar_H_3}
	H(s) = \frac{Y(s)}{U(s)} = \frac{1}{m s^2 + b s + k}
\end{equation}


Ahora el enunciado pide obtener la transferencia partiendo de las matrices A,B,C y D.
Por teoría sabemos que se obtiene de la siguiente expresión: 

\begin{equation}
	H(s) = C(sI - A)^{-1}B + D
\end{equation}


Partiendo de la relación entre la función de transferencia y la
representación en espacio de estados:


Para el sistema masa–resorte–amortiguador se tiene:

\begin{equation}
	A=
	\begin{bmatrix}
		0 & 1\\
		-\frac{k}{m} & -\frac{b}{m}
	\end{bmatrix},
	\quad
	B=
	\begin{bmatrix}
		0\\
		\frac{1}{m}
	\end{bmatrix},
	\quad
	C=
	\begin{bmatrix}
		1 & 0
	\end{bmatrix},
	\quad
	D=0
\end{equation}

Primero se calcula la matriz \(sI-A\):

\begin{equation}
	sI-A =
	\begin{bmatrix}
		s & 0\\
		0 & s
	\end{bmatrix}
	-
	\begin{bmatrix}
		0 & 1\\
		-\frac{k}{m} & -\frac{b}{m}
	\end{bmatrix}
	=
	\begin{bmatrix}
		s & -1\\
		\frac{k}{m} & s+\frac{b}{m}
	\end{bmatrix}
\end{equation}

Luego se calcula su inversa:

\begin{equation}
	(sI-A)^{-1} =
	\frac{1}{s\left(s+\frac{b}{m}\right)+\frac{k}{m}}
	\begin{bmatrix}
		s+\frac{b}{m} & 1\\
		-\frac{k}{m} & s
	\end{bmatrix}
\end{equation}

Multiplicando por \(B\):

\begin{equation}
	(sI-A)^{-1}B =
	\frac{1}{s\left(s+\frac{b}{m}\right)+\frac{k}{m}}
	\begin{bmatrix}
		s+\frac{b}{m} & 1\\
		-\frac{k}{m} & s
	\end{bmatrix}
	\begin{bmatrix}
		0\\
		\frac{1}{m}
	\end{bmatrix}
	=
	\frac{1}{s\left(s+\frac{b}{m}\right)+\frac{k}{m}}
	\begin{bmatrix}
		\frac{1}{m}\\
		\frac{s}{m}
	\end{bmatrix}
\end{equation}

Finalmente, multiplicando por \(C\):

\begin{equation}
	H(s)=
	\begin{bmatrix}
		1 & 0
	\end{bmatrix}
	\frac{1}{s\left(s+\frac{b}{m}\right)+\frac{k}{m}}
	\begin{bmatrix}
		\frac{1}{m}\\
		\frac{s}{m}
	\end{bmatrix}
\end{equation}

\begin{equation}
	H(s)=
	\frac{1}{m}
	\frac{1}{s^2+\frac{b}{m}s+\frac{k}{m}}
\end{equation}

Multiplicando numerador y denominador por \(m\):

\begin{equation}
	H(s)=\frac{1}{ms^2+bs+k}
\end{equation}

 \subsection{Dos grados de libertad}
 
 
 \begin{figure}[H]
 	\centering
 	\includegraphics[width=0.5\linewidth]{img/masas_resorte_2grados_Lib}
 	\caption{}
 	\label{fig:masasresorte2gradoslib}
 \end{figure}
 
 
 \section{Red RLC de tercer orden}
 
 \begin{figure}[H]
 	\centering
 	\includegraphics[width=0.5\linewidth]{img/redRLC}
 	\caption{}
 	\label{fig:redrlc}
 \end{figure}
 
 
 \section{Motor de continua }
 
 \begin{figure}[H]
 	\centering
 	\includegraphics[width=0.5\linewidth]{img/motor}
 	\caption{}
 	\label{fig:motor}
 \end{figure}
 
 
 
 \section{Tanques de agua acoplados}
 
 
 \begin{figure}[H]
 	\centering
 	\includegraphics[width=0.5\linewidth]{img/tanques_acoplados}
 	\caption{}
 	\label{fig:tanquesacoplados}
 \end{figure}
 

\end{document}